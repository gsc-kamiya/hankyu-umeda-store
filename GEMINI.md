# GEMINI.md — Project Configuration for Gemini CLI (Google)

Use the same content as `CLAUDE.md`. The two files exist so different agent CLIs find their expected file at the repo root.

See `CLAUDE.md` in this `templates/agent-config/` directory for the full template — copy it here and rename.
